family: fourierkan

stage_a_inputs:
  tune_config: configs/experiment_stages/stage_a_performance_tuning/stage_a_fourierkan/fourierkan_tune.yaml
  candidate_manifest: sweeps/stage_a/fourierkan/stage-a-fourierkan-tune_candidates.json

stage_b_controls:
  top_k: 5
  seeds: [13, 29, 47]
  selection_name: stage-b-fourierkan-shortlist
  output_experiment_prefix: stage-b-fourierkan
  qwk_tolerance: 0.01

stage_b_outputs:
  retrain_manifest: artifacts/retrain/fourierkan/stage-b-fourierkan-shortlist/manifest.json
  selection_manifest: artifacts/selection/fourierkan_selection.json

stage_c_materialized_configs:
  best_performance: configs/experiment_stages/stage_c_explanation_package/materialized/fourierkan_best_performance.yaml
